# Flask 웹 서버 및 템플릿 엔진
Flask>=2.0
Jinja2>=2.9
# AWS S3(Wasabi) 연동
boto3
# Firebase Admin SDK (Firestore + Storage 접근용)
firebase-admin
# QR 코드 생성
qrcode
Pillow
# HTTP 요청
requests
# 비디오 처리 (moviepy가 imageio-ffmpeg 등을 내부에서 사용)
moviepy>=2.2.1
imageio-ffmpeg
# 수치 연산 · 데이터 처리
numpy
# Pandas 및 OpenPyXL: 엑셀(.xlsx) 읽기/쓰기용
pandas>=1.5
openpyxl>=3.0
# 날짜/시간 파싱 (python-dateutil)
python-dateutil
# 데코레이터 유틸리티 (Flask나 기타 라이브러리에서 종속될 수 있음)
decorator