# Firestore/GCS 접근
firebase-admin

# 엑셀(.xlsx) 생성/편집
pandas>=1.5
openpyxl>=3.0

# (선택) ISO8601, 날짜 처리 등에 쓰였다면
python-dateutil

# (만약 service_account.from_service_account_info 로 오류가 난다면
# google-auth 계열을 명시적으로 추가)
google-auth
google-auth-oauthlib
google-auth-httplib2
